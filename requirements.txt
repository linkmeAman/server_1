# Dynamic Multi-Project API Requirements

# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# ASGI server
python-multipart==0.0.6

# Environment variables
python-dotenv==1.0.0

# Database
sqlalchemy==2.0.23
pymysql==1.1.0
aiomysql==0.2.0
alembic==1.13.3
cryptography==41.0.7

# Logging (already in Python standard library)
# No additional packages needed for basic logging

# HTTP Client
httpx==0.25.1

# Security
pyseto==1.7.6
bcrypt==4.1.3
PyYAML==6.0.2

# Cache (PRISM Phase 3)
# redis>=4.2 ships redis.asyncio as a built-in submodule — no [asyncio] extra needed
redis>=4.2.0

# Read-only DB explorer
sqlparse==0.5.3
pandas==2.2.3

# Google Reviews — GMB API + Semantic Analysis
google-api-python-client==2.120.0
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
vaderSentiment==3.3.2
transformers==4.40.0
# Install CPU-only torch separately to keep image size down:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
langdetect==1.0.9
scikit-learn==1.4.2
boto3==1.36.0